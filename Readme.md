# Проект «Киноман»
Проект онлайн‑курса «JavaScript. Архитектура клиентских приложений».

## Техническое задание
### Проект: Киноман
О проекте: «Киноман» — сервис для фанатов большого кино. Подробная информация о горячих новинках кино, возможность выбрать и сформировать собственный список фильмов к просмотру, обсуждение кинофильмов и многое другое. «Киноман» — поможет провести время интересно.

#### 1. Описание функциональности
##### 1.1 Общий контейнер
В правом верхнем углу шапки отображается звание пользователя. Звание зависит от количества просмотренных фильмов, вычисляется при загрузке приложения и может изменяться в ходе использования пользователем приложения (при добавлении или удалении фильмов из просмотренных).

0 — блок со званием не отображается;
от 1 до 10 — novice;
от 11 до 20 — fan;
от 21 и выше — movie buff.
В правом углу подвала выводится информация о количестве фильмов в сервисе. Информация обновляется один раз — при загрузке приложения.

##### 1.2 Фильмы
После загрузки приложения в списке отображается не более 5 карточек фильмов.

Показ оставшихся фильмов выполняется нажатием на кнопку «Show more». При нажатии показываются очередные 5 фильмов или оставшиеся фильмы, если их количество меньше 5.

После показа всех карточек с фильмами, кнопка «Show more» скрывается.

Любое изменение фильтра или сортировки сбрасывает счётчик показанных фильмов и отсчёт начинается заново.

В случае отсутствия фильмов вместо списка отображается текст: «There are no movies in our database».

##### 1.3 Карточка фильма
Карточки фильмов представлены в двух вариантах: стандартный (в списке фильмов) и расширенный (отдельный попап с описанием фильма).

В стандартном варианте карточка с фильмом содержит следующую информацию:

Постер (картинка);
Название фильма;
Рейтинг;
Год релиза;
Продолжительность в формате часы минуты (например «1h 36m»);
Жанр;
Краткое описание (не более 140 символов);
Количество комментариев;
Если описание фильма больше 140 символов, то в карточке отображается 139 символов описания и знак многоточие (…).

В карточке фильма отображается блок с кнопками управления:

«Add to watchlist» — добавляет/удаляет фильм из списка к просмотру;
«Already watched» — помечает фильм как просмотренный/непросмотренный;
«Add to favorites» — добавляет/удаляет фильм в избранное.
Клик по карточке фильма (за исключением кликов по кнопкам управления) открывает попап с подробной информацией о фильме;

Попап содержит расширенную информацию о фильме:

Полноразмерная обложка;
Название фильма;
Оригинальное название фильма;
Рейтинг;
Режиссёр;
Сценаристы;
Актёрский состав;
Дата и год релиза в формате день месяц год (например: «01 April 1995»);
Продолжительность в формате часы минуты (например «1h 36m»);
Страна;
Жанр (ы);
Полное описание;
Возрастной рейтинг;
Фильм может относиться к нескольким жанрам. Если фильм относится к нескольким жанрам, выводите «Genres», иначе «Genre».

В попапе отображается блок с кнопками управления:

«Add to watchlist» — добавляет/удаляет фильм из списка к просмотру;
«Already watched» — помечает фильм как просмотренный/непросмотренный;
«Add to favorites» — добавляет/удаляет фильм в избранное.
В заголовке «Comments» отображается количество комментариев к фильму. Например: «Comments 8».

Любое изменение информации о фильме в попапе должно отображаться в списке фильмов мгновенно. При изменении информации попап не должен закрываться самовольно.

Попап можно закрыть нажатием на кнопку закрытия в правом верхнем углу (крестик) или нажатием на клавиатуре кнопки «Esc». При закрытии попап удаляется из DOM.

Одновременно может быть открыт только один попап. При открытии нового попапа прежний закрывается, например при клике на другую карточку при открытом попапе. Несохранённые изменения (неотправленный комментарий) пропадают.

##### 1.4 Комментарии
Список комментариев к фильму и форма добавления нового комментария доступны в попапе. Комментарии загружаются при открытии попапа.

Каждый комментарий состоит из:

Текст комментария;
Эмоция;
Автор комментария;
Дата комментария;
Кнопка удаления.
Дата комментария отображается в формате год/месяц/день часы:минуты (например «2019/12/31 23:59»).

Для добавления нового комментария пользователь заполняет текст комментария и выбирает эмоцию (один вариант из: smile, sleeping, puke, angry). Имя автора формируется случайным образом на сервере, с клиента оно не передаётся. Дата также приходит с сервера.

Введённые пользователем данные экранируются.

Отправка формы осуществляется нажатием комбинации клавиш Ctrl/Command + Enter.

Пользователь может удалить произвольный комментарий. Комментарий удаляется нажатием на кнопку «Delete», расположенную в блоке с комментарием.

##### 1.5 Рейтинг
Пользователь никак не может влиять на оценку фильма.
Рейтинг фильма высчитывается на сервере.
##### 1.6 Фильтры
В приложении предусмотрено несколько фильтров:

«All movies» — все фильмы;
«Watchlist» — фильмы, добавленные в список к просмотру (Watchlist);
«History» — просмотренные фильмы (Already watched);
«Favorites» — фильмы, добавленные в избранное (Favorites).
Количество фильмов, соответствующих фильтру отображается справа в элементе с фильтром. Для фильтра «All movies» количество не отображается.

Информация о количестве фильмов, соответствующих каждому фильтру доступна сразу, без необходимости применения фильтра.

Фильтр должен переключаться при клике и на надпись, и на счётчик.

Если фильтру соответствует больше 5 фильмов, то в списке фильмов по этому фильтру отображается первые 5 фильмов, а остальные отображаются по нажатию на кнопку «Show more», как и в списке фильмов «All movies».

Если список фильмов был отфильтрован, и какой-то из фильмов перестал соответствовать критериям фильтрации (например пользователь убрал отметку «Add to favorites» в списке по фильтру «Favorites»), карточка этого фильма должна быть моментально удалена из списка. Это удаление карточки не должно каким-либо образом ломать логику кнопки «Show more» или нарушать порядок сортировки.

Если в отфильтрованном списке были удалены все карточки, вместо списка отображается соответствующий текст. Например, если пользователь убрал отметки «Add to favorites» во всех фильмах в списке по фильтру «Favorites», должна появиться заглушка «There are no favorite movies now». Все фразы приведены в файле в директории с разметкой.

##### 1.7 Сортировка
Пользователю доступна возможность сортировки фильмов по дате выхода (клик по ссылке «Sort by date») и рейтингу (клик по ссылке «Sort by rating»). Сортировка работает в одном направлении — от максимального к минимальному: при сортировке по дате выхода в начале списка будут самые новые фильмы, при сортировке по рейтингу — с самым высоким рейтингом.

Для отмены сортировки и возвращения к исходному порядку пользователь кликает по ссылке «Sort by default».

При смене фильтра сортировка сбрасывается на состояние «Sort by default».

Сортировка не должна отображаться, если нет фильмов для сортировки.

#####  1.8 Взаимодействие с сервером
Сервер расположен по адресу: https://17.ecmascript.htmlacademy.pro/cinemaddict/;
Все запросы, которые отправляются серверу, должны содержать заголовок Authorization со значением Basic ${случайная строка}. Например, Basic er883jdzbdw. Случайная строка формируется однократно при старте приложения.
Интерфейс должен реагировать на отправку любого запроса к серверу. Примеры реакции описаны в соответствующих пунктах ТЗ.
При отправке комментария, форма, содержащая текст комментария должна быть заблокирована.
Если запрос на отправку комментария выполнился успешно, то комментарий должен быть добавлен в список комментариев. Форму добавления комментария нужно очистить и разблокировать.
При возникновении ошибки в момент отправки комментария, форма, содержащая текст комментария, должна быть разблокирована и к ней применяется эффект «покачивание головой». Стили для эффекта есть в проекте.
Обновление любого элемента в DOM происходит только после успешного выполнения запроса к серверу.
Ниже описаны структуры данных, которые отдаёт или принимает сервер, и доступные методы для обращения. Путь, указанный рядом с методом, — это часть адреса, по которому нужно обращаться к серверу. Например, по адресу https://17.ecmascript.htmlacademy.pro/cinemaddict/movies можно сделать GET-запрос, чтобы получить все фильмы, доступные в приложении.
1.8.1 Ресурсы
Структуры данных
LocalComment:
{
"comment": "a film that changed my life, a true masterpiece, post-credit scene was just amazing omg.",
"emotion": "smile"
}
Comment:
{
"id": "42",
"author": "Ilya O'Reilly",
"comment": "a film that changed my life, a true masterpiece, post-credit scene was just amazing omg.",
"date": "2019-05-11T16:12:32.554Z",
"emotion": "smile"
}
Comment.emotion — одно из следующих значений:
["smile", "sleeping", "puke", "angry"]
Movie:
{
"id": "0",
"comments": [
$Comment.id$, $Comment.id$
],
"film_info": {
"title": "A Little Pony Without The Carpet",
"alternative_title": "Laziness Who Sold Themselves",
"total_rating": 5.3,
"poster": "images/posters/blue-blazes.jpg",
"age_rating": 0,
"director": "Tom Ford",
"writers": [
"Takeshi Kitano"
],
"actors": [
"Morgan Freeman"
],
"release": {
"date": "2019-05-11T00:00:00.000Z",
"release_country": "Finland"
},
"runtime": 77,
"genre": [
"Comedy"
],
"description": "Oscar-winning film, a war drama about two young people, from the creators of timeless classic \"Nu, Pogodi!\" and \"Alice in Wonderland\", with the best fight scenes since Bruce Lee."
},
"user_details": {
"watchlist": false,
"already_watched": true,
"watching_date": "2019-04-12T16:12:32.554Z",
"favorite": false
}
}
AuthorizationError:
{
"error": 401,
"message": "Header Authorization is not correct"
}
NotFoundError:
{
"error": 404,
"message": "Not found"
}
Фильмы /movies
GET /movies
Код ответов
200 OK
401 Unauthorized
Пример:

Request:

URL: GET /movies
Headers: Authorization: Basic kTy9gIdsz2317rD
Response:

Status: 200 OK
Body: массив, содержащий элементы типа Movie
Request:

URL: GET /movies
Response:

Status: 401 Unauthorized
Body: структура AuthorizationError
PUT /movies/: film_id
Выставление рейтинга, добавление в избранное.

Обратите внимание, изменять можно только пользовательскую информацию. То есть то, что находится внутри поля user_details.

Код ответов
200 OK
401 Unauthorized
404 Not found
Пример:

Request:
URL: PUT /movies/11
Headers: Authorization: Basic er883jdzbdw
Body: Структура вида Movie
Response:
Status: 200 OK
Body: Структура вида Movie
Комментарии /comments/:film_id
GET /comments/: film_id
Получение комментариев к фильму.

Код ответов:
200 OK
401 Unauthorized
404 Not found
Пример:

Request:
URL: GET /comments/11
Headers: Authorization: Basic er883jdzbdw
Response:
Status: 200 OK
Body: Массив, содержащий элементы вида Comment
POST /comments/: film_id
Создание нового комментария к фильму.

Код ответов:
200 OK
401 Unauthorized
404 Not found
Пример:

Request:
URL: POST /comments/11
Headers: Authorization: Basic er883jdzbdw
Body: Структура вида LocalComment
Response:
Status: 200 OK
Body: Структура вида { movie: Movie, comments: [Comment, Comment] }
DELETE /comments/: comment_id
Удаление существующего комментария.

Код ответов:
200 OK
401 Unauthorized
404 Not found
Пример:

Request:
URL: DELETE /comments/42
Headers: Authorization: Basic er883jdzbdw
Response:
Status: 200 OK
##### 1.9 Обратная связь интерфейса
На время загрузки вместо карточек фильмов нужно вывести информационное сообщение.

При нажатии на кнопку удаления комментария «Delete» её заголовок изменяется на «Deleting...», а сама кнопка блокируется. Если при выполнении запроса к серверу возникла ошибка, заголовок нужно вернуть к изначальному — «Delete», а кнопку разблокировать.

В момент отправки запроса на создание комментария форма блокируется от внесения изменений. Разблокировка формы происходит после завершения выполнения запроса (неважно, успешно выполнен запрос или нет).

Если запрос не удалось выполнить (сервер недоступен, произошла ошибка), форма создания остаётся открытой, к ней применяется эффект «покачивание головой».

Обновление элементов (удаление комментариев, обновление информации о фильме и так далее) в DOM происходит после успешного выполнения запроса к серверу.

#### 2. Дополнительные задания
Обратите внимание, если вы планируете оживление блоков «Top rated movies» и «Most commented», добавьте эти блоки в разметку в вашем проекте.

В блоках «Top rated movies» и «Most commented» отображается по две карточки фильмов. В блоке «Top rated movies» — фильмы с наивысшим рейтингом. В блоке «Most commented» — фильмы с наибольшим количеством комментариев. Если у всех фильмов одинаковый рейтинг или одинаковое количество комментариев, берутся два случайных фильма соответственно.

Блок «Top rated movies» не отображается, если у всех фильмов рейтинг равен нулю.

Блок «Most commented» не отображается, если отсутствуют фильмы с комментариями.

Блок «Most commented» обновляется во время работы с приложением при добавлении или удалении пользователем комментариев.

Реализуйте вывод даты публикации комментария в человеческом формате (например «now», «a few minutes ago», «8 years ago» и т. д.) вместо формата, предложенного в пункте 1.4 техзадания.

#### 3. Разное
В зависимости от состояния некоторым элементам управления применяются соответствующие классы оформления. Например, активный фильтр и т. д. Примеры доступны в директории с вёрсткой (markup).

### 1.8. Начинаем программировать

Настройка сборки
Настройте сборку проекта так, чтобы зависимости в JS собирались при помощи сборщика модулей Webpack. Для этого выполните следующие шаги:

Установите из npm пакеты webpack и webpack-cli.

Обратите внимание, здесь и далее нужно устанавливать конкретные версии зависимостей, указанные в файле requirements.md в вашем репозитории.

Создайте в корне проекта файл webpack.config.js и опишите конфигурацию сборки:

задайте main.js точкой входа;
в качестве директории для сборки укажите папку build. Помните, что путь должен быть абсолютный. Используйте path.resolve;
файл сборки (бандл) назовите bundle.js;
активируйте опцию очистки директории для сборки перед новой сборкой;
активируйте генерацию source-maps.
Установите из npm пакет copy-webpack-plugin. Это плагин для Webpack, который будет копировать файлы из /public в /build и туда же подкладывать бандл. После установки нужно настроить плагин в webpack.config.js.

Добавьте в package.json скрипт с именем build со значением webpack --mode production. С его помощью мы будем собирать код для публикации проекта с помощью Webpack.

Собираемые с помощью Webpack файлы не должны попасть в репозиторий, поэтому добавьте их в .gitignore.

Настройка сервера для разработки
Чтобы на каждое изменение кодовой базы не собирать бандл и не открывать public/index.html в браузере руками, настроим сервер для разработки.

Установите из npm пакет webpack-dev-server.

Добавьте в package.json скрипт с именем start для запуска сервера. Значением будет команда запуска сервера и настройки: webpack serve --mode development --open.

Теперь достаточно выполнить команду npm start. Браузер с нужным адресом откроется автоматически. Если этого не произойдёт, откройте приложение вручную. Адрес будет указан в терминале после запуска команды. А если вас, наоборот, раздражает, что браузер открывается сам, удалите настройку --open из команды.

Настройка транспайлера
Если мы хотим использовать в исходном коде новейшие возможности JavaScript, нужно настроить Babel. Это транспайлер — инструмент, который автоматически превращает современный код в код, поддерживаемый нужными браузерами.

Установите из npm основной пакет @babel/core.

Установите плагин @babel/preset-env. В нём содержатся инструкции о том, в какой поддерживаемый код можно перевести новейшие конструкции.

Создайте файл настроек babel.config.json и укажите в нём используемый плагин.

Установите лоадер babel-loader и подключите его в webpack.config.js.

Добавьте в package.json секцию browserslist с указанием браузеров, которые мы хотим поддерживать. Именно на этот список будет смотреть плагин @babel/preset-env, подбирая инструкции для транспиляции.

Если какие-то настройки вызывают трудности, загляните в демо-проект.

### 1.2 — Начинаем программировать

Любое дело начинается с подготовки рабочего места. Программирование — не исключение. Подготовим наше «рабочее место» фронтендера, для этого нам потребуется настроить сборку, сервер для разработки и транспайлер.

Настройка сборки
Фронтендерам больше хочется писать код и меньше думать о том, как этот код (а ещё и различные ассеты!) внедрить на страницу. Для этих целей существует сборка. Для сборки мы используем программу Webpack.

Webpack возьмёт на себя все заботы о том, чтобы наш красивый модульный JavaScript был собран в удобный для загрузки бандл, чтобы разметка и все нужные файлы попали в директорию build, которую нам останется только выгрузить на сервер.

Для установки программы нужно выполнить команду:

npm i webpack webpack-cli -DE
Программа состоит из двух частей: webpack — сам сборщик, webpack-cli — модуль, который позволит работать с Webpack в терминале.

Обратите внимание, после установки пакетов из npm в вашем проекте автоматически должны измениться файлы package.json и package-lock.json.

Теперь нам нужно настроить Webpack. Сделаем это с помощью JavaScript. Для этого создадим файл webpack.config.js. Это не обычный JavaScript, а серверный — Node.js, поэтому логика описания настроек своебразная. Но не переживайте, синтаксис серверного JavaScript и обычного — одинаковый.

В файле webpack.config.js перечислим все настройки, что от нас требуют. В итоге файл с настройками должен выглядеть следующим образом:

const path = require('path'); // Подключаем модуль Node.js для работы с путями на компьютере

module.exports = {
entry: './src/main.js', // Указывает точку входа - наш главный JavaScript-файл проекта
output: {
filename: 'bundle.js', // Задаём название итоговому бандлу
path: path.resolve(__dirname, 'build'), // Указываем, куда нужно положить бандл после создания
clean: true, // Отмечаем, что перед сохранением обновлённого бандла старые файлы нужно удалить
},
devtool: 'source-map' // Аквтивируем генерацию source-maps
};
Базовая настройка завершена.

Единственное, о чём мы пока не подумали, о не JavaScript-файлах вроде index.html, картинок, стилей, шрифтов и прочем. Их тоже нужно сложить в итоговую директорию /build, иначе какой толк от приложения, где только JavaScript? Такой сайт работать не будет.

Для этих целей — сборки не JavaScript-файлов — к Webpack существует дополнение (более широкое название — «плагин») copy-webpack-plugin. Это плагин будет за нас копировать файлы вроде index.html, картинок, стилей, шрифтов из /public в /build.

Устанавливаем плагин командой:

npm i copy-webpack-plugin -DE
И после настраиваем его в webpack.config.js:

const path = require('path');
+const CopyPlugin = require("copy-webpack-plugin"); // Подключаем плагин

module.exports = {
entry: './src/main.js',
output: {
filename: 'bundle.js',
path: path.resolve(__dirname, 'build'),
clean: true,
},
devtool: 'source-map',
+  plugins: [
+    new CopyPlugin({
+      patterns: [{ from: 'public' }], // Добавляем плагин в сборку и указываем директорию, откуда нужно скопировать файлы
+    }),
+  ]
   };
   Теперь, когда настройка завершена, остаётся лишь добавить команду для запуска сборки. По договорённости среди разработчиков такая команда называется build. Опишем её в секции команд в package.json:

{
...
"scripts": {
"build": "webpack --mode production"
}
...
}
Обратите внимание, что мы вызываем команду webpack с опцией --mode production. Так мы запускаем внутренние механизмы оптимизации сборки Webpack вроде минификации кода.

Давайте запустим нашу сборку npm run build и посмотрим на результат:

Результат сборки

Как видите, кроме бандла bundle.js в директорию /public попали нужные нам файлы.

Обратите внимание, что собранные файлы не должны попасть в Git, потому что они только здесь и сейчас нам для проверки, что наш код работает. Поэтому добавьте директорию /build в список игнорирования для Git .gitignore.

Настройка сервера для разработки
Хорошо, сборка работает, но как посмотреть на результат этой работы вживую? Можно, конечно, после каждой сборки выгружать /public на сервер и смотреть на приложение в браузере. Это долго и неудобно, поэтому разработчики придумали вариант попроще — сервер для разработки. Точно такой же сервер, как тот, куда вы бы выгрузили ваш сайт, только на вашем компьютере и без необходимости что-то куда-то выгружать.

Нам подойдёт тот сервер для разработки, который поддерживается самим Webpack. Процесс нам уже знаком. Устанавливаем программу:

npm i webpack-dev-server -DE
После добавляем команду для её запуска. По договорённости среди разработчиков такая команда называется start. Опишем её в секции команд в package.json:

{
...
"scripts": {
"start": "webpack serve --mode development --open"
}
...
}
Давайте запустим наш сервер npm run start и посмотрим на результат:

Результат сборки

Браузер с нужным адресом откроется автоматически. Если этого не произошло, откройте приложение вручную, адрес появится в терминале после запуска команды:

Результат сборки

Настройка транспайлера
Фронтендеры любят всё новое, и если мы хотим в исходном коде использовать новейшие возможности JavaScript, нужно настроить транспайлер. Это программа, которая автоматически превращает современный код в код, поддерживаемый нужными браузерами.

Например мы хотим использовать в коде оператор опциональной последовательности, который пока поддерживают не все браузеры.

В коде мы пишем:

const obj = {
a: {
b: false
},
};

const c = obj?.a?.b?.c;
А в бандл попадает что-то вроде:

var _obj$a, _obj$a$b;

const obj = {
a: {
b: false
}
};

const c = obj === null || obj === void 0 ? void 0 : (_obj$a = obj.a) === null || _obj$a === void 0 ? void 0 : (_obj$a$b = _obj$a.b) === null || _obj$a$b === void 0 ? void 0 : _obj$a$b.c;
Это Babel постарался.

Процесс нам уже знаком. Устанавливаем программу и дополнения к ней:

npm i @babel/core @babel/preset-env -DE
Создаём файл настроек babel.config.json (кстати, он тоже может быть в формате *.js как настройки Webpack, а не *.json) и указываем в нём используемый плагин:

{
"presets": ["@babel/preset-env"]
}
Теперь дело за малым — у нас есть сборщик, у нас есть транспайлер. Нужно их подружить, чтобы Webpack в процессе сборки с помощью Babel преобразовывал наш код.

Для этих целей у Webpack есть помощник (более широкое название — «лоадер») babel-loader. Этот лоадер будет использовать Babel в процессе сборки для преобразования нашего кода.

Устанавливаем плагин командой:

npm i babel-loader -DE
И после настраиваем его в webpack.config.js:

const path = require('path');
const CopyPlugin = require("copy-webpack-plugin");

module.exports = {
entry: './src/main.js',
output: {
filename: 'bundle.js',
path: path.resolve(__dirname, 'build'),
clean: true,
},
devtool: 'source-map',
plugins: [
new CopyPlugin({
patterns: [{ from: 'public' }],
}),
],
+  module: {
+    rules: [
+        {
+          test: /\\.js$/, // Указываем регулярным выражением, что преобразовывать нужно только JavaScript-файлы
+          exclude: /(node_modules)/, // Уточняем, что файлы из директории node_modules трогать не нужно
+          use: ['babel-loader'] // Указываем, какой лоадер использовать
+        }
+    ]
     }
     };
     Остается буквально один вопрос: а как Babel будет понимать, какой код преобразовывать, а какой нет? Здесь нам поможет инструмент Browserslist.

Он по умолчанию включён в Babel. Всё, что нам нужно, указать список браузеров, в которых должно работать наше приложение. Сделать это можно в package.json:

{
...
"browserslist": [
"last 2 versions",
"not dead",
"not ie <= 11",
"not op_mini all",
"not < 0.25%"
],
...
}
Готово. Теперь можно писать код.

### 1.9. Время разбивать камни

Первая ваша задача будет заключаться в «нарезке» вёрстки на отдельные компоненты (элементы интерфейса). Компоненты мы будем отрисовывать динамически с помощью JavaScript. Для этого мы начнём знакомство с View и Presenter из MVP.

Ознакомьтесь с разметкой в директории markup.

Откройте файл public/index.html. В этом файле приведена общая разметка страницы (layout) и присутствуют комментарии с описанием элементов, которые должны быть добавлены в данную область. Перед закрывающим тегом body с помощью тега script подключите bundle.js.

Функцию для отрисовки (вставки в DOM) компонентов писать не нужно, она уже есть в вашем репозитории. Только разберитесь с её интерфейсом.

Создайте модули (отдельные файлы) и опишите в них классы для генерации DOM-элементов. Эти модули — наши компоненты, или же View в рамках MVP. Поэтому:

для них нужно завести отдельную директорию src/view;
именуйте файлы как существительные;
для экспорта классов используйте экспорт по умолчанию.
Большое путешествие
Киноман
На сколько компонентов и на какие именно компоненты разбить страницу решите самостоятельно, или воспользуйтесь предложенным списком:

фильтры;
карточка фильма (в списке);
звание пользователя;
кнопка «Show more»;
подробная информация о фильме (попап).
Заведите презентер, который будет отвечать за логику отрисовки компонентов, и отрисуйте компоненты по одному экземпляру на страницу, кроме:

компонент «Карточка фильма» отрисовывается в списке 5 раз.
Запустите сервер для разработки. Откройте сайт локально в браузере. Все компоненты должны быть корректно отрисованы. Для проверки сравните полученный результат с разметкой в директории markup.

### 1.3 — Время разбивать камни

Динамический сайт от статичного отличает то, что части страницы перерисовываются на лету с помощью JavaScript. Перерисовывать будет проще, если разбить цельную страницу на независимые части — компоненты.

Пословица «Семь раз отмерь, один отрежь» здесь тоже работает, поэтому прежде, чем что-то разбивать, давайте посмотрим на готовые (цельные) страницы в директории /markup.

Смысл в том, чтобы глазами выделить изменяемые части от статичных. В будущем каждая из таких «изменяемых» частей станет отдельным компонентом.

Например, если мы сравним главную страницу list.html:

Главная страница

И страницу без дополнительных блоков list-no-extra.html:

Страница без допполнительных блоков

У нас появятся первые два компонента — блоки Top rated и Most commented.

А если затем посмотрим страницу загрузки, станет понятно, что список фильмов — тоже компонент:

Страница загрузки

Заглянув после на страницу с применёнными фильтрами list-filter.html:

Страница с фильтрами

Станет ясно, что карточка с фильмом — сама по себе компонент, а блоки Top rated, Most commented и список — так, контейнеры для отрисовки.

Таким образом изучим все страницы, заглянем в техзадание (узнать, что на странице меняется) и составим список компонентов:

Карточка. Для удобства можно доразбить на:
Описание
Кнопки
Попап. Для удобства можно доразбить на:
Описание
Кнопки
Список комментариев
Форма добавления комментария
Кнопка «Показать ещё»
Статистика (в подвале)
Звание пользователя в шапке
Фильтры
Сортировка
Список (универсальный компонент для списка фильмов и блоков Top rated, Most commented)
Фильмы (общий список)
Контент страницы
Что такое компоненты с точки зрения JavaScript? Отдельные файлы. Поэтому под каждый компонент нужно будет завести файл. Так как мы выбрали архитектурный паттерн MVP, а компоненты ближе всего к представлению (View), все файлы нужно сложить в директорию /src/view. Так будет удобнее ориентироваться в проекте.

Каждый компонент — это в первую очередь разметка и JavaScript для её оживления и превращения из HTML в DOM-элемент. Удобнее всего, если все View будут похожи друг на друга по структуре. Саму структуру мы придумывать не будем, возьмём из учебного проекта:

// Вспомогательная функция для превращения строки с HTML в DOM-элементы
import {createElement} from '../render.js';

// Функция, которая будет возвращать строку с HTML-разметкой компонента
const createTemplate = () => '...';

// Клас - он же сам компонент
export default class View {
getTemplate() {
return createTemplate(); // Кстати, можно не создавать отдельную createTemplate функцию, а сразу возвращать строку с HTML-разметкой компонента из метода класса
}

getElement() {
if (!this.element) {
this.element = createElement(this.getTemplate());
}

    return this.element;
}

removeElement() {
this.element = null;
}
}
Для удобства стоит называть класс и функцию с HTML менее абстрактно. Например, FilmButtonMoreView и createFilmButtonMoreTemplate соответственно для компонента кнопки «Показать ещё».

Разметку же возьмём из директории /markup. Пока нас устроит статичная разметка, прямо с данными. В итоге у нас появится десяток файлов компонентов, и каждый похож друг на друга:


Давайте попробуем отрисовать несколько наших View на странице. Начнём с простых — звания пользователя и статистики в подвале.

Функция для отрисовки нам предоставлена, нужно только разобраться в ней:

const render = (component, container, place = RenderPosition.BEFOREEND) => {
container.insertAdjacentElement(place, component.element);
};
Интерфейс нам подсказывает, что первым аргументом функция ждёт компонент component, затем контейнер для отрисовки container и опционально позицию отрисовки.

Компонентом в этом случае выступает экземпляр нашего класса, в качестве контейнера нужно найти определённый DOM-элемент, а позицию подскажет разметка в /markup.

Например, чтобы отрисовать звание пользователя в шапке смотрим разметку list.html.

В метод createTemplate компонента в качестве HTML-разметки «уйдёт» section.header__profile:

Метод компонента

Контейнером выступит header.header:

Контейнер

Место вставки — перед закрывающим тегом RenderPosition.BEFOREEND.

Давайте оформим эту логику в JavaScript в точке входа:

import HeaderProfileView from './view/header-profile-view.js';

import {render} from './render.js';

const siteHeaderElement = document.querySelector('.header');

render(new HeaderProfileView(), siteHeaderElement);
Запустим локальный сервер npm run start и посмотрим на результат:

Результат

Как видим, всё там, где должно быть.

Аналогично поступим с фильтрами и статистикой в подвале. Посмотрим на результат:

Результат

В заключении решим задачу посложнее — отрисовку списка. Список фильмов — мозг нашего приложения. В дальнейшем при отрисовке списка появится много условий «это рисуй тогда, это по условию, это вообще другой раз», поэтому простым вызовом render() в точке входа не обойтись. Для этих целей в MVP существует презентер (Presenter). Презентеры мы будем хранить в директории /src/presenter, как несложно догадаться.

Как в случае с View, удобнее всего, если все Presenter будут похожи друг на друга по структуре. Пока в нашем приложении нет логики, достаточно чтобы презентер по нашему хотению отрисовывал все компоненты списка фильмов: карточки, попап, кнопку «Показать ещё», сортировку. Для этого презентеру нужно знать о всех этих компонентах, создать их экземпляры и самому вызвать render() для них:

Создаём экземпляры

Если вам кажется, что все эти render() можно было легко описать в точке входа, то вам не кажется. Но это был бы шаткий фундамент будущего приложения, поэтому давайте сразу «строить на века».

Дальше дело за малым, создаём экземпляр нашего презентера в точке входа и повелеваем ему отрисовать всё, за что он отвечает, вызвав .init():

const filmsPresenter = new FilmsPresenter();

filmsPresenter.init(siteMainElement);
Запустим локальный сервер npm run start и посмотрим на итоговый результат:

Итоговый результат

Тесненько и всё не по-настоящему, но это мы поправим в будущих заданиях!

### 2.9. Шаблонизируй это
В этом задании мы отделим данные от представления: избавимся от статического контента в шаблонах компонентов и заведём в приложении для каждой сущности подходящую структуру данных.

Чтобы понять, что должно оказаться в данных, а что нет, задайте себе вопрос: «Есть ли смысл скачивать эту информацию с сервера отдельно, может ли она измениться?»

Не стоит заводить структуру данных, которая описывает размеры логотипа или статический текст шаблона. В структурах должны храниться только те данные, которыми мы будем оперировать в проекте: получать их с сервера, изменять, отправлять на сервер обратно.

Большое путешествие
Киноман
Заведите структуру данных, которая опишет фильм:

его название;
постер и описание;
комментарии;
и так далее... Что должно быть в структуре, можно узнать из технического задания.
Обязательно выделите комментарии в отдельную структуру данных: с эмоцией, датой, автором и сообщением комментария. Не копируйте одну структуру в другую, вместо этого свяжите их друг с другом по ключу.

Создайте временный модуль, задачей которого будет генерация готовых объектов с информацией по описанным структурам.

Заведите в приложении Model и заполните её временными данными.

Исправьте View карточки фильма так, чтобы контент ожидался снаружи компонента.

Аналогичным образом перепишите View попапа с подробной информацией о фильме.

Передайте Model в Presenter и там отрисуйте компоненты уже на основе полученных «рыбных» данных.

Откуда брать «рыбные» данные?
Названия фильмов можно взять с постеров, а можете взять из списка своих любимых фильмов.

Постер (название файла) — случайное изображение из директории /public/images/posters.

В качестве описания можно взять 1–5 случайных предложений из текста:

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Cras aliquet varius magna, non porta ligula feugiat eget. Fusce tristique felis at fermentum pharetra. Aliquam id orci ut lectus varius viverra. Nullam nunc ex, convallis sed finibus eget, sollicitudin eget ante. Phasellus eros mauris, condimentum sed nibh vitae, sodales efficitur ipsum. Sed blandit, eros vel aliquam faucibus, purus ex euismod diam, eu luctus nunc ante ut dui. Sed sed nisi sed augue convallis suscipit in sed felis. Aliquam erat volutpat. Nunc fermentum tortor ac porta dapibus. In rutrum ac purus sit amet tempus.

        Копировать



Главное в работе с «рыбой» — не заморачиваться и помнить, что это временное решение, поэтому оно должно быть максимально дешёвым.

### 2.1 — Шаблонизируй это
Цель задания — подготовить наши View к работе с настоящими данными, которые в будущем мы будем получать с сервера. Остальное вторично, поэтому не стесняемся подглядывать в техзадание и разметку.

В пункте 1.3 техзадания перечислена вся информация, которая будет приходить с сервера:

Информация с сервера

В пункте 1.8.1 показаны структуры данных:

Структуры данных

Наша задача — повторить их с минимальными затратами, пока мы не научимся работать с сервером. Решать задачу будем в три приёма: опишем функцию-генератор данных, заведём модель (Model) в приложении, научим View отрисовывать данные извне.

Генерация данных
Проще всего начинать не с чистого листа, поэтому первым делом возьмём структуру из техзадания и обернём её в функцию-генератор, предварительно создав файл /src/mock/film.js:

const generateFilm = () => ({
title: "A Little Pony Without The Carpet",
alternative_title: "Laziness Who Sold Themselves",
total_rating: 5.3,
poster: "images/posters/blue-blazes.jpg",
age_rating: 0,
director: "Tom Ford",
writers: [
"Takeshi Kitano"
],
actors: [
"Morgan Freeman"
],
release: {
date: "2019-05-11T00:00:00.000Z",
release_country: "Finland"
},
runtime: 77,
genre: [
"Comedy"
],
description: "Oscar-winning film, a war drama about two young people, from the creators of timeless classic \\"Nu, Pogodi!\\" and \\"Alice in Wonderland\\", with the best fight scenes since Bruce Lee."
});
В целом задача решена, дальше нужно её детализировать, нам же хочется данных как с сервера, а не чтобы во всём приложении был один и тотже фильм?!

Поэтому дальше начинаем рандомизировать наши данные. Подход здесь одинаковый, поэтому давайте рассмотрим на примере заголовка title:

Заводим константу с возможными названиями фильмов
const titles = [
'Country On Him',
'Raiders With The Carpet',
'Guest Who Sold The Darkness',
...
];
Описываем функцию, которая будет брать случайное название
const getRandomValue = (items) => ...;
Записываем результат работы функции в данные
const generateFilm = () => ({
title: getRandomValue(titles),
alternative_title: "Laziness Who Sold Themselves",
total_rating: 5.3,
...
});
Повторяем для остальных данных.
Остановимся на нюансах, которые нужно учесть…

Первое, в нашем коде мы не можем использовать snake_case согласно критериям, поэтому нужно будет в нашей структуре заменить весь snake_case на camelCase. Было alternative_title стало alternativeTitle.

Второе, одной функции getRandomValue(), которая возвращает случайный элемент переданного массива, будет недостаточно. У нас как миниму есть числовые данные, а значит для них потребуется какие-то функции вроде getRandomInteger() или getRandomFloat() — функцию по получению случайного целого или дробного числа соответственно. Проще всего найти такие функции в интернете и не тратить время на их написание с нуля.

Третье, структура может быть вложенная. Сценаристы writers и актёры actors — это массив имён. Информация о выходе release вообще объект. Вложенные структуры заполняются аналогичным образом, просто нужно быть чуть внимательнее.

Информация о фильме — это самая большая и сложная часть структуры, но кроме неё мы должны заполнить поля id и comments:

{
id: 0,
comments: [ ... ],
film_info: { ... },
}
id — это просто порядковое число, начнём с него:

const generateFilms = () => {
// Создаем массив с данными о фильмах
const films = Array.from({length: FILM_COUNT}, generateFilm);

/*
На его основе создаем массив с объектами нужной нам структуры
{
id,
comments,
film_info,
}
Пока только с id и film_info
*/
return films.map((film, index) => {
return {
id: String(index + 1), // id - просто порядковый номер
filmInfo: film,
};
});
};
comments — массив таких же порядковых чисел, это будут id комментариев.

const generateFilms = () => {
const films = Array.from({length: FILM_COUNT}, generateFilm);

+  // Ключ totalCommentsCount нужен нам для того, чтобы у фильмов не повторялись id комментариев, ведь не может быть, чтобы один комментарий относился к нескольким фильмам
+  let totalCommentsCount = 0;

return films.map((film, index) => {
+    const filmCommentsCount = getRandomInteger(1, MAX_COMMENTS_ON_FILM);

+    // Логика такая: суммируем все id комментариев...
+    totalCommentsCount += filmCommentsCount;

    return {
      id: String(index + 1),
+      comments: Array.from(
+        {length: filmCommentsCount},
+        // ...и раздаём их по порядку с конца
+        (_value, commentIndex) => String(totalCommentsCount - commentIndex)
+      ),
      filmInfo: film,
  };
  });
  };
  Всё хорошо, только мы не учли ситуацию, когда комментариев к фильму нет вовсе. Для этого можно добавить простое условие: если комментарии нужны, генерируем их id; если не нужны, подставляем пустой массив.

const generateFilms = () => {
const films = Array.from({length: FILM_COUNT}, generateFilm);

let totalCommentsCount = 0;

return films.map((film, index) => {
+    const hasComments = getRandomInteger(0, 1);

+    const filmCommentsCount = (hasComments)
+      ? getRandomInteger(1, MAX_COMMENTS_ON_FILM)
+      : 0;

  totalCommentsCount += filmCommentsCount;

  return {
  id: String(index + 1),
+      comments: (hasComments)
+        ? Array.from(
+            {length: filmCommentsCount},
+            (_value, commentIndex) => String(totalCommentsCount - commentIndex)
+          )
+        : [],
      filmInfo: film,
  };
  });
  };
  Похожим образом, но более простым, описываем функцию по генерации комментариев в /src/mock/comment.js:

const generateComment = () => ({ ... });

const getCommentCount = (films) => films.reduce(
(count, film) => count + film.comments.length, 0
);

const generateComments = (films) => {
const commentCount = getCommentCount(films);

return Array.from({length: commentCount}, (_value, index) => {
const commentItem = generateComment();

    return {
      id: String(index + 1),
      ...commentItem,
    };
});
};
Обратите внимание на getCommentCount() — функцию подсчета, сколько нужно комментариев. Можно, конечно, нагенерировать комментариев впрок, но зачем? Проще посчитать, сколько сгенерировалось id для комментариев при генерации данных фильмов, и потом создать ровно столько же комментариев.

Как итог у нас с вами есть два модуля для генерации моковых данных фильмов и комментариев:

Генерация моковых данных

Прежде, чем переходить к заключительной части, давайте убедимся, что наши функции-генераторы работают. Для этого импортируем их в точке входа и выведем результат их работы в консоль:

import {generateFilms} from './mock/film';
import {generateComments} from './mock/comment';

...

const films = generateFilms();
const comments = generateComments(films);

console.log(films);
console.log(comments);
Результат в консоли

Теперь, когда данные генерируются, нужно выстроить финальную цепочку MVP — соединить Model и Presenter. В свою очередь Presenter и View уже соединены.

Заведём модели фильмов и комментариев. Модели мы будем хранить в директории /src/model, как несложно догадаться. Как в случае с Presenter и View, удобнее всего, если все Model будут похожи друг на друга по структуре.

Пока в нашем приложении нет логики, достаточно чтобы модели вызывали функции-генераторы данных и возвращали результат. Например как выглядит код модели фильмов:

import {generateFilms} from '../mock/film.js';

export default class FilmsModel {
films = generateFilms();

get = () => this.films;
}
Теперь, когда у нас есть модели, их экземпляры нужно передать в Presenter. Удобнее всего это сделать в точке входа:

const filmsModel = new FilmsModel();
const commentsModel = new CommentsModel(filmsModel);

const filmsPresenter = new FilmsPresenter();

filmsPresenter.init(siteMainElement, filmsModel, commentsModel);
Теперь только остаётся поддержать эти изменения в самом презентере, исправив код метода .init():

Изменения

Обратите внимание, что мы теперь для отрисовки карточек и комментариев используем данные из моделей. Только вот незадача, компонентам мы данные передаём, но получать они их не умеют!

Поэтому заключельной точкой задания мы научим наши компоненты принимать данные. Здесь всё просто: мы при создании экземпляра компонента передаём ему данные, значит нужно поддержать их получение в конструкторе класса и передать дальше в метод, который создаёт DOM-элемент на основе HTML-разметки.

Рассмотрим на примере компонента карточки:

export default class FilmCardView {
+  constructor(film) {
+    this.film = film;
+  }

getTemplate() {
+    return createFilmCardTemplate(this.film);
     }

getElement() { ... }

removeElement() { ... }
}
А дальше дело техники. createFilmCardTemplate() возвращает HTML-разметки в виде шаблонной строки, поэтому мы можем просто интерполировать данные:

Интерполяция данных

Готово. Давайте запустим наш сервер и посмотрим, что при каждой загрузке страницы данные для компонентов генерируются автоматически и случайным образом:

### 3.8. Революция или эволюция? (часть 1)

В этом задании мы начнём по максимуму использовать современные возможности классов, а в проекте реализуем первую интерактивность интерфейса.

Небольшой рефакторинг
Рефакторинг — это переписывание кода для большей простоты и понятности. В процессе рефакторинга не изменяется поведение программы, не правятся ошибки, не добавляется новая функциональность.

Измените ваши классы в проекте с учётом новых знаний о геттерах, сеттерах и приватных свойствах классов. Заглядывайте в демо-проект, если не знаете, с чего начать.
Большое путешествие
Киноман
Настоящая информация о фильме
Теперь, когда наш код прекрасен, реализуем показ и скрытие попапа с подробной информацией о фильме.

Избавьтесь от кода, который сразу отрисовывает попап с подробной информацией о фильме, если такой код у вас был. С этого момента при загрузке приложения должны отображаться только карточки с фильмами.

В месте, где вы создаёте компонент карточки, а потом его отрисовываете, создайте компонент с попапом. Теперь у вас есть два компонента: карточка фильма и попап с подробной информацией о фильме.

Найдите ссылки в компоненте карточки фильма и кнопку закрытия попапа (крестик) во втором компоненте (не нужно ходить за ними в document, используйте геттер элемента). Навесьте на них пустые обработчики события click.

Реализуйте в добавленных обработчиках показ и скрытие попапа с подробной информацией о фильме.

Обратите внимание, чтобы не было двойного скролла, при открытии попапа на <body> нужно добавлять класс hide-overflow, а при закрытии удалять его.

В отличие от демо-проекта и «Большого путешествия» показ и скрытие попапа удобнее сделать с помощью appendChild и removeChild, а не replaceChild.

В дополнение к обработчику события click на кнопке закрытия попапа добавьте на страницу обработчик нажатия клавиши «Esc», который будет скрывать попап. Механизм скрытия остаётся прежний.

На примере проекта «Киноман» вы можете посмотреть, как должен выглядеть проект после выполнения этого задания.


### 3.1 — Революция или эволюция? (часть 1)
Архив проекта

Небольшой рефакторинг
Цель этого задания проста — использовать в проекте современные возможности JavaScript. Конечно, в реальном мире разработчики сразу пишут современный код, но то опытные разработчики, а мы с вами только учимся. Поэтому запасаемся внимательностью и делаем свойства классов приватными, если к ним доступ нужен снаружи — снабжаем геттерами и сеттерами. С методами похожая история, только если метод нужен снаружи, как .init() презентера, просто оставляем его публичным.

Настоящая информация о фильме
В прошлых заданиях мы создали компонент попапа и даже научились его отображать данные, пусть пока и ненастоящие. Только есть одна проблема, сейчас попап отображается сразу при заходе на страницу:

Отображение попапа

А по условиям техзадания попап должен появляться по клику на карточку в списке. Такие условия — это и есть логика приложения, под которую мы в первом разделе весьма удачно заложили фундамент — наш презентер.

Первым делом подготовим рабочее место — избавимся от кода, который сразу отрисовывает попап с подробной информацией о фильме:

export default class FilmsPresenter {
...

init = (container, filmsModel, commentsModel) => {
this.container = container;
this.filmsModel = filmsModel;
this.commentsModel = commentsModel;

    this.films = [...filmsModel.get()];

    render(this.sortComponent, this.container);
    render(this.filmsComponent, this.container);
    render(this.filmListComponent, this.filmsComponent.getElement());
    render(this.filmListContainerComponent, this.filmListComponent.getElement());

    for (let i = 0; i < this.films.length; i++) {
      render(new FilmCardView(this.films[i]), this.filmListContainerComponent.getElement());
    }

    render(this.filmButtonMoreComponent, this.filmListComponent.getElement());

    const comments = [...this.commentsModel.get(this.films[0])];

-    render(new FilmDetailsView(this.films[0], comments), this.container.parentElement);
     };
     }
     Попап больше не отрисовывается, однако сильно лучше не стало — метод .init() перегружен, и это мы ещё не добавили логики в него!

Поэтому следующим логичным шагом выглядит его разбивка на отдельные методы с говорящими названиями: отрисовка карточки фильма #renderFilm() и отрисовка попапа #renderFilmDetails().

Теперь в эти методы можно перенести создание экземпляров View и вызовы render(). Сюда же мы допишем нужную логику, которая связана с отрисовкой этих двух компонентов, а именно что клик по карточке вызывает отрисовку попапа:

export default class FilmsPresenter {
#renderFilm(film, container) {
const filmCardComponent = new FilmCardView(film);

+    const linkFilmCardElement = filmCardComponent.element.querySelector('a');

+    linkFilmCardElement.addEventListener('click', () => {
+      this.#renderFilmDetails(film);
+    });

    render(filmCardComponent, container.element);
}
}
А клик по крестику в попапе приводит к его закрытию:

export default class FilmsPresenter {
#renderFilmDetails(film) {
const comments = [...this.#commentsModel.get(film)];

    this.#filmDetailsComponent = new FilmDetailsView(film, comments);

+    const closeButtonFilmDetailsElement = this.#filmDetailsComponent.element.querySelector('.film-details__close-btn');

+    closeButtonFilmDetailsElement.addEventListener('click', () => {
+      this.#filmDetailsComponent.element.remove();
+      this.#filmDetailsComponent = null;
+    });

    render(this.#filmDetailsComponent, this.#container.parentElement);
}
}
Запускаем локальный сервер и проверяем, что всё работает:


Работать — работает, только мы не учли все требования техзадания: страница «за попапом» продолжает скролиться, попап не закрывается по нажатию на клавишу Esc. Что можно с этим сделать? Можно «в лоб» добавить нужный нам код в существующие обработчики, но тогда они разрастутся и со временем понять, что там происходить будет сложно. Не наш вариант, поэтому мы заведём в классе еще несколько методов с говорящими названиями:

export default class FilmsPresenter {
...

// Метод, который мы будем вызывать по клику на карточку.
// Вбирает в себя все действия, необходимые для корректного показа попапа
#addFilmDetailsComponent = (film) => { ... };

// Обратный метод по скрытию попапа
#removeFilmDetailsComponent = () => { ... };

// Метод, который мы будем вызывать по нажатию на клавишу Esc,
// а он в свою очередь вызывать #removeFilmDetailsComponent
// Можно ли было обойтись без этого метода? Нет!
// Потому что событие keydown для всех клавиш, а нам нужна только Esc,
// а значит появятся всякие условия, которым не место в #removeFilmDetailsComponent
#onEscKeyDown = (evt) => { ... };
}
Дальше нам останется лишь вызвать эти «говорящие методы» в обработчиках событий:

Методы в обработчиках событий

По клику на карточку для открытия вызываем #addFilmDetailsComponent()

Вызов #addFilmDetailsComponent()

По клику на крестик или по нажатию Esc для скрытия вызываем #removeFilmDetailsComponent()

Всё необходимые действия теперь у нас собраны в двух методах, и если в будущем потребуется по открытию или сокрытию сделать что-то ещё, мы знаем куда добавить эти пару строк JavaScript.

Запускаем локальный сервер и проверяем, что всё работает на этот раз как надо:
